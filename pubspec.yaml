name: precords_android
description: "PRECORDS – Football Players & Clubs Management"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0" 

dependencies:
  flutter:
    sdk: flutter

  # Core
  dio: ^5.9.0
  flutter_secure_storage: ^10.0.0
  get: ^4.7.3
  flutter_dotenv: ^6.0.0
  get_storage: ^2.1.1

  # UI & Utilities
  cupertino_icons: ^1.0.8
  cached_network_image: ^3.4.1
  image_picker: ^1.1.2          # For picking player photo only
  flutter_animate: ^4.5.0

  fluttertoast: ^9.0.0  # Latest as of late 2025/6
  palette_generator: ^0.3.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env





